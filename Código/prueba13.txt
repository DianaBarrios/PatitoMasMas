programa pelos;
var
  int i, j, k;

funcion int uno(int c);
var
  int d;
{
  c = d + (k - d*2+d);
  si (d > 0) entonces {
    regresa(uno(c-d));
  } sino {
    regresa(d+k);
  }
}

funcion void dos(int a, int b);
var
  int x, y;
{
    x = a + b;
  si (x > b*k) entonces {
      y = uno(x);
      escribe(y);
  } sino {
      escribe("otra");
  }
}

principal(){
  lee(k);
  i = 5;
  j = i * 2 + i;
  dos(j-i,j);
}
