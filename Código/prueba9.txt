programa patito9;
var
  int otra[2],varAux;
  float wow;
  
  funcion int suma(int a,int b);
  var int res;
  {
    si (a > b) entonces {
      regresa(a);
    } sino {
      regresa(b);
    }
  }

funcion void dos(int w);
  var int y;
  {
    si (w > 5) entonces{
      y = suma(5,7);
      escribe(y,w);
    } sino {
      escribe(w,"llamada recursiva");
      dos(w+1);
    }
  }
  
  principal(){
    dos(1);
  }
