programa factorialCiclico;
var
  int x, arr[7][6], mat[7][6], vfc[7][6], aux;
  char car;
  float valor;
  int another;
funcion int factorial(int n, float a, char c);
var 
    int i, res;
{
    res = 1;
  desde i = 1 hasta n+1 hacer {
      res = res*i;
  }
  escribe(n,a,c);
  regresa(res);
}
principal(){
    escribe("Escribe un numero:");
    lee(x);

    valor = 5.6;
    car = 'c';
    another = 100;

    aux = factorial(x,valor,car);

    escribe("factorial de x:", aux);

    vfc = 100;
    
    arr = 10;

    escribe("arr:");
    escribe(arr);
    mat = 5;
    escribe("vfc");
    escribe(vfc);

    escribe("suma de matrices");
    mat = arr + vfc;
    escribe(mat);
}