programa factorialRecursivo;
var
  int x;
funcion int factorial(int n);
var 
    int aux;
{
   si (n == 0 || n == 1) entonces {
       regresa(1);
   } sino {
       regresa(n * factorial(n-1));
   }
}
principal(){
    escribe("Escribe un numero:");
    lee(x);
    escribe("El factorial es:");
    escribe(factorial(x));
}
